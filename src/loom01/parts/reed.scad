include <../variables.scad>;
use <../loom_v1.scad>;

reed(count=dents,
     height=reed_height,
     dpi=dpi,
     thickness=reed_thickness,
     bar_height=reed_bar_height,
     bar_thickness=reed_bar_thickness,
     ridge_height=reed_ridge_height,
     ridge_thickness=reed_ridge_thickness
     );
